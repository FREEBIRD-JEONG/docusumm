name: Summary Worker Cron

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  run-worker:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Trigger summary worker endpoint
        env:
          APP_URL: ${{ secrets.APP_URL }}
          CRON_SECRET: ${{ secrets.CRON_SECRET }}
        run: |
          if [ -z "$APP_URL" ]; then
            echo "APP_URL secret is missing"
            exit 1
          fi

          if [ -n "$CRON_SECRET" ]; then
            curl --fail --show-error --silent \
              --request POST \
              --header "Authorization: Bearer $CRON_SECRET" \
              "$APP_URL/api/internal/summary-worker"
          else
            curl --fail --show-error --silent \
              --request POST \
              "$APP_URL/api/internal/summary-worker"
          fi
